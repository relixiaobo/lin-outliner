# Tenon Design System Calibration Audit

This file records the current renderer UI design-system calibration ledger. It is
not a new rule layer. Rules still live in the kernel, foundations, components,
patterns, surfaces, and implementation files; this audit classifies findings and
points to the evidence that proves each decision.

## Classification Model

| Class | Meaning | Required response |
| --- | --- | --- |
| Code drift | Product code or a guard violates the design-system contract. | Fix code or guard behavior, then add evidence. |
| Spec drift | The spec contradicts current intended product behavior or another owning rule. | Fix the smallest owning spec layer; do not change UI just to satisfy stale prose. |
| Named exception | A local exception is intentional, scoped, and evidenced. | Keep it named in the registry or metrics exception table; fail stale exceptions. |
| Open design decision | The right rule is directional, taste-bearing, hard to reverse, or product-changing. | Do not guess. Record the decision point and escalate before implementation. |

## Calibration Rule

When code and spec disagree, the answer is not automatically "make code obey the
current spec." First decide the owner:

1. If the product behavior is correct and the prose is stale, fix the spec.
2. If the spec expresses a reusable rule and code diverges locally, fix the code.
3. If the behavior is valid but local, name it as an exception with evidence.
4. If the choice changes product behavior or taste, treat it as an open design
   decision and stop before implementation.

Any standard adjustment must improve reuse, consistency, or measurability. A
standard change that only hides one local violation is not accepted.

## Findings Ledger

| ID | Finding | Class | Resolution | Evidence |
| --- | --- | --- | --- | --- |
| CA01 | Component coverage counted JSX-like text in comments and missed several documented primitives. | Code drift | Metrics now use the TypeScript AST and map documented component contracts to real JSX/export names. | `scripts/design-system-metrics.ts`; `bun scripts/design-system-metrics.ts --check` |
| CA02 | Raw-hex enforcement only scanned CSS while renderer TS/TSX could still carry source literals. | Code drift | Raw-hex scan now covers renderer CSS, TS, and TSX with a named source-literal exception path. | `scripts/design-system-metrics.ts`; raw hex outside tokens = 0 |
| CA03 | Product-surface native controls were not separated from primitive implementation internals. | Code drift | Metrics distinguish product direct native use, named native-control exceptions, and primitive implementation internals. | `scripts/design-system-metrics.ts --json` |
| CA04 | Native-control exceptions were counted but not reported by file/reason, and stale entries could survive. | Code drift | Metrics now report `exceptedNativeFiles` with counts/reasons and fail stale exception entries. | `scripts/design-system-metrics.ts --json`; `staleNativeControlExceptions: []` |
| CA05 | Provider-ready empty agent conversations had conflicting rules: greeting text in one spec path, blank current product behavior elsewhere. | Spec drift | The spec now follows current product/test behavior: provider-ready empty conversations stay visually blank; no-provider onboarding remains explicit. | `patterns.md`, `surfaces.md`, `tests/e2e/agent-onboarding.spec.ts` |
| CA06 | Destructive interaction guidance overgeneralized neutral hover and conflicted with solid destructive confirmation buttons. | Spec drift | Interaction states now distinguish ordinary destructive affordance hover from solid destructive confirmation buttons. | `patterns.md`, `components.md`, `foundations.md` |
| CA07 | Tag color code still allowed raw/alias palette paths while the design system wanted tokenized, theme-aware tags. | Code drift | Tag storage now uses a closed token palette; invalid raw/alias values are rejected/fall back. | `src/core/configSchema.ts`, `src/renderer/ui/tags/tagColors.ts` |
| CA08 | Tag color docs did not clearly describe the closed token palette and invalid raw/alias fallback behavior. | Spec drift | Foundations now describe the closed tag palette and fallback behavior. | `foundations.md` |
| CA09 | Focus/radius token gaps left valid CSS values looking like one-off literals. | Code drift | Added/used shared tokens for `--focus-ring-shadow-inset` and `--radius-none`. | `src/renderer/styles/tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA10 | Pointer cursor policy needed tighter enforcement for non-link inline reference render paths. | Code drift | Non-link agent inline references are gated away from pointer affordance; cursor guard covers pointer declarations and representative controls. | `tests/e2e/cursor-affordances.spec.ts` |
| CA11 | Representative runtime surface coverage did not include enough complex overlays, settings panes, previews, and agent detail surfaces. | Code drift | Runtime guard now covers shell, onboarding, settings panes, config child windows, overlays, date picker, file/image previews, agent process details, and run details in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA12 | Material surfaces relied on convention that every `backdrop-filter` uses the shared material token. | Code drift | Token guard now fails any local backdrop filter outside `var(--material-backdrop)` or explicit `none`. | `tests/e2e/typography-tokens.spec.ts` |
| CA13 | Some comments still described historical rollout phases instead of the current shared-token system. | Spec drift | Comments now describe current shared material fallback and current SettingsInsetList reuse. | `src/renderer/styles/a11y.css`, `agent-dock.css`, `outliner.css`, `popover-command.css`, `shell.css`, `SettingsInsetList.tsx` |
| CA14 | Outliner trigger popovers and code-block chrome were high-interaction surfaces but not represented in the shared runtime guard. | Code drift | Runtime guard now opens tag suggestions, slash commands, reference suggestions, and the code-block language menu in light/dark without changing outliner layout behavior. | `tests/e2e/design-system-runtime.spec.ts` |
| CA15 | Shared menu and floating-tool chrome was implemented through primitives but not opened by the runtime design-system guard. | Code drift | Runtime guard now opens row/sidebar context menus, batch tag selector, floating text toolbar, file-preview pill menu, and image-row action menu in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA16 | Agent composer interaction chrome had static coverage but not runtime coverage for its menus and suggestion surfaces. | Code drift | Runtime guard now opens the channel picker, channel options menu, mention suggestions, model menu, and reasoning flyout in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA17 | View configuration and schema definition surfaces were covered by focused behavior specs but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens view-toolbar sort/filter popovers, the definition config panel, and the definition picker in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA18 | Document preview outline navigation had focused behavior coverage but no shared runtime design-system coverage. | Code drift | Runtime guard now opens an EPUB full preview and its document outline rail/popover in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA19 | Shared confirm, inline-file menu, and composer error surfaces were implemented through product paths but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the destructive confirm dialog, inline-file context menu, and composer attachment error status in light/dark through real UI entry points. | `tests/e2e/design-system-runtime.spec.ts` |
| CA20 | Tag badge context actions hand-rolled menu DOM instead of the shared menu primitive and had no runtime design-system coverage. | Code drift | Tag badge actions now use `MenuSurface`/`MenuItem` with an accessible menu label and runtime guard coverage in light/dark. | `src/renderer/ui/tags/TagBar.tsx`; `tests/e2e/design-system-runtime.spec.ts` |
| CA21 | Dream manual-run dialog was behavior-tested but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the Dream channel manual-run dialog in light/dark through the channel picker and launcher surface. | `tests/e2e/design-system-runtime.spec.ts` |
| CA22 | Agent message metadata/details popover was behavior-tested but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens an assistant message Details dialog through the native context-menu bridge in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA23 | The separate global launcher renderer used the shared token system but was absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens `launcher.html` with a launcher IPC mock in light/dark while preserving the named native-glass exception for the real Electron window. | `tests/e2e/design-system-runtime.spec.ts`; `docs/spec/launcher.md` |
| CA24 | The day-panel date navigation calendar was distinct from field date pickers but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the panel date calendar in light/dark through the main day panel shell. | `tests/e2e/design-system-runtime.spec.ts` |
| CA25 | Agent usage hover cards were portaled tooltip surfaces but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the assistant message usage tooltip in light/dark through the real Details hover path. | `tests/e2e/design-system-runtime.spec.ts` |
| CA26 | Field-name reuse popovers were implemented through shared popover primitives and behavior-tested, but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the field-name reuse popover in light/dark through the real outliner field creation path, without changing field-row layout or interaction behavior. | `tests/e2e/design-system-runtime.spec.ts` |
| CA27 | Options and reference field-value popovers were implemented through shared popover primitives and behavior-tested, but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the option-value picker and reference-value node search in light/dark through real outliner field-value entries, without changing field-value layout or interaction behavior. | `tests/e2e/design-system-runtime.spec.ts` |
| CA28 | View-toolbar display and group configuration popovers were behavior-tested, while the shared runtime design-system guard only opened sort and filter. | Code drift | Runtime guard now opens display, group, sort, and filter view-configuration popovers in light/dark through the real toolbar. | `tests/e2e/design-system-runtime.spec.ts` |
| CA29 | Full preview header action menus used the shared menu primitive and focused file-preview behavior tests, but runtime design-system coverage only opened inline preview menus. | Code drift | Runtime guard now opens a file reader split pane and its header action menu in light/dark through the real preview flow. | `tests/e2e/design-system-runtime.spec.ts` |
| CA30 | View-toolbar tooltips were behavior-tested but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens a view-toolbar tooltip in light/dark through hover on the real toolbar. | `tests/e2e/design-system-runtime.spec.ts` |
| CA31 | Settings row action menus used the shared anchored menu primitive and behavior-tested triggers, but runtime design-system coverage only rendered settings rows closed. | Code drift | Runtime guard now opens a configured-provider row action menu in light/dark through the real Settings Providers surface. | `tests/e2e/design-system-runtime.spec.ts` |
| CA32 | Agent debug usage tooltips were behavior-tested but absent from the shared runtime design-system guard. | Code drift | Runtime guard now opens the debug run details pane and hovers the Call details usage tooltip in light/dark. | `tests/e2e/design-system-runtime.spec.ts` |
| CA33 | Selected option field-value popovers were behavior-tested, while runtime coverage only opened the empty-field option picker. | Code drift | Runtime guard now selects an option value and opens the selected-field-options popover in light/dark through the real field-value row. | `tests/e2e/design-system-runtime.spec.ts` |
| CA34 | Inline file hover previews were behavior-tested but absent from the shared runtime design-system guard, which only opened the inline file context menu. | Code drift | Runtime guard now inserts a local-file mention and opens its hover preview popover in light/dark through the real composer flow. | `tests/e2e/design-system-runtime.spec.ts` |
| CA35 | The composer model submenu was behavior-tested through model selection flows, while runtime coverage only opened the parent model menu and reasoning submenu. | Code drift | Runtime guard now opens the model submenu in light/dark through the real composer model control. | `tests/e2e/design-system-runtime.spec.ts` |
| CA36 | The implementation validation summary lagged behind the expanded runtime surface matrix. | Spec drift | Validation now summarizes the covered categories, including row menus, selected field values, inline-file preview/context, file-preview header menus, composer submenus, and agent/debug tooltips. | `implementation.md`, `tests/e2e/design-system-runtime.spec.ts` |
| CA37 | The runtime surface guard had many representative cases, but the metrics report could not quantify the matrix or fail when the matrix became undiscoverable. | Code drift | Metrics now report runtime surface cases and light/dark theme checks, and `--check` fails if the matrix is missing or empty. | `scripts/design-system-metrics.ts --json`; `tests/e2e/design-system-runtime.spec.ts` |
| CA38 | The design system prohibited scale-pop feedback, but no guard stopped a renderer stylesheet from reintroducing `transform: scale(...)`. | Code drift | Token/static CSS coverage now fails any scale transform in renderer styles, keeping interaction feedback layout-stable. | `tests/e2e/typography-tokens.spec.ts` |
| CA39 | OS-driven theming and the removed `--primary` token family were design-system rules, but live CSS could reintroduce `[data-theme]` selectors or `--primary` token references without a static failure. | Code drift | Token/static CSS coverage now fails renderer `[data-theme]` selectors and live `--primary` token definitions/references. | `tests/e2e/typography-tokens.spec.ts` |
| CA40 | Functional-state neutrality was documented, but a hover/active/focus/selected selector could still paint its fill, border, or ring with brand, link, or status colour tokens without a static failure. | Code drift | Token/static CSS coverage now fails chromatic semantic tokens in functional-state fills, borders, and rings, preserving only the solid destructive-confirmation exception. | `tests/e2e/typography-tokens.spec.ts`; `components.md` Button contract |
| CA41 | Dark appearance was specified as one OS-driven token layer plus named scoped exceptions, but a new stylesheet could add its own `prefers-color-scheme: dark` branch without review. | Code drift | Token/static CSS coverage now fails unregistered dark-media rules; the allowlist is limited to the central dark token layer, Shiki generated colours, and the documented panel blend-mode correction. | `tests/e2e/typography-tokens.spec.ts`; `foundations.md` Appearance |
| CA42 | The reduced-motion layer overclaimed a single global reset even though a few perpetual activity indicators intentionally become static states under reduced motion. | Spec drift | The rule now reads as a global baseline plus registered local static-state replacements, and token/static CSS coverage fails unregistered reduced-motion media rules. | `a11y.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA43 | Inline file previews carried a local reduced-transparency fallback even though they already used the shared material tokens owned by the central accessibility layer. | Code drift | Removed the redundant local fallback and added a reduced-transparency media allowlist limited to the central accessibility layer plus the named system-launcher glass exception. | `inline-ref.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA44 | Increased contrast had a central token layer, but no guard stopped component styles from adding unregistered local `prefers-contrast: more` branches. | Code drift | Token/static CSS coverage now fails unregistered increased-contrast media rules, preserving the central accessibility layer and the named system-launcher glass exception. | `tests/e2e/typography-tokens.spec.ts`; `a11y.css` |
| CA45 | The z-index ladder existed as `--z-*` tokens, but new global stacking values could still be hard-coded in product styles. | Code drift | Token/static CSS coverage now fails raw global z-index values while preserving local 0/1/2 stacking-context layers. | `tests/e2e/typography-tokens.spec.ts`; `tokens.css` |
| CA46 | The document outline mini-rail intentionally hid its tiny scrollbar, but the exception was not named and no guard stopped content scroll containers from doing the same. | Named exception | The kernel now names the mini-rail scrollbar exception, and token/static CSS coverage fails hidden scrollbars anywhere else. | `design-system.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA47 | Typography rules prohibited viewport-driven font sizing, but the guard did not check font token declarations for viewport/container units. | Code drift | Token/static CSS coverage now fails viewport or container units in `font-size` declarations and `--font-*` token declarations. | `tests/e2e/typography-tokens.spec.ts`; `foundations.md` Typography |
| CA48 | The typography rule only prohibited CJK letter-spacing, leaving room for local tracked labels and codes that split the type rhythm. | Spec drift | Foundations now require product `letter-spacing: 0`, the remaining nonzero declarations were removed, and token/static CSS coverage fails future nonzero letter-spacing. | `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA49 | A few content elements used `--material-popover` as a plain fill, and one tooltip used it without the shared backdrop-filter pair. | Code drift | Message attachments and edit cards now use opaque/neutral content fills, the debug tooltip carries the shared material backdrop pair, and token/static CSS coverage fails future unpaired `--material-*` backgrounds. | `agent-message.css`; `agent-debug.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA50 | The native text-selection policy had runtime cursor coverage, but no guard stopped a future global `user-select: none !important` from suppressing content selection. | Code drift | Cursor-affordance coverage now fails forced user-selection suppression outside active drag/resize gesture selectors. | `tests/e2e/cursor-affordances.spec.ts`; `base.css` |
| CA51 | The reusable bare-input primitive removed the UA outline and also suppressed the shared keyboard focus ring, forcing every future consumer to remember a local override. | Code drift | `.input-bare:focus-visible` now only removes the UA outline and inherits the global modality-gated keyboard focus ring; cursor-affordance coverage fails a future local `box-shadow: none` regression. | `input.css`; `tests/e2e/cursor-affordances.spec.ts` |
| CA52 | A few level-1 popovers still mixed overlay shadows with `--outline-subtle`, contradicting the pure-shadow overlay rule and making elevation depend on an extra stroke. | Code drift | Dream manual, inline file-preview, and agent activity popovers now use pure level-1 shadows; token/static CSS coverage fails future overlay shadow + outline-token mixes. | `agent-composer.css`; `inline-ref.css`; `agent-message.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA53 | Live CSS still used a generic `--shadow` alias for level-1 elevation, hiding whether a surface was a rail, menu/popover, dialog, thumb, or local content elevation. | Code drift | Removed the alias and rewrote live uses to `--overlay-shadow-level-1`, preserving visual output while making elevation tier explicit and guardable. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA54 | OS-driven appearance had dark-media and `[data-theme]` guards, but no guard kept local `color-scheme:` declarations inside the foundation layer or the registered document light-canvas exception. | Code drift | Token/static coverage now fails unregistered renderer `color-scheme:` declarations across CSS/TS/TSX. | `tests/e2e/typography-tokens.spec.ts`; Kernel Exception Registry |
| CA55 | `.command-palette` shared the translucent popover material rule even though the spec classifies the in-app command palette as an opaque level-2 focused surface. | Code drift | Command palette CSS now uses `--bg-elevated` + level-2 shadow with no backdrop filter, and token/static coverage fails future material/backdrop regressions on `.command-palette`. | `popover-command.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA56 | ConfirmDialog used the content-surface alias even though dialogs are level-2 focused overlays, which made the dark dialog read like the canvas instead of an elevated modal. | Code drift | ConfirmDialog now uses `--bg-elevated`, and the level-2 overlay guard covers command palette, ConfirmDialog, and run-detail drawer surfaces. | `confirm-dialog.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA57 | The motion rules prohibited layout instability, but no guard stopped future `transition: all` or new width/height/spacing/position transitions from slipping into product CSS. | Code drift | Token/static coverage now fails layout-affecting transitions unless they are registered: workspace canvas rail padding and the absolute indent-guide stroke width. | `tests/e2e/typography-tokens.spec.ts`; `canvas.css`; `outliner.css` |
| CA58 | The foundation token file had a single-source-of-truth principle, but no guard would catch a duplicated token definition inside `tokens.css`. | Code drift | Token/static coverage now fails duplicate custom-property definitions in the foundation token file while leaving component-scoped local variables alone. | `tests/e2e/typography-tokens.spec.ts`; `tokens.css` |
| CA59 | Token rules allowed component CSS to write local `rgba()` even though current renderer CSS already routes functional colors through tokens or `color-mix()` with token inputs. | Spec drift | Foundations now keep raw functional color literals inside token declarations, and token/static coverage fails future non-token `rgb()` / `rgba()` / `hsl()` / `hsla()` usage. | `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA60 | Live CSS referenced undefined foundation tokens (`--motion-slow`, `--line-ui`, `--fill-selected`, `--font-sans`, `--fill-hover`, `--space-7`), making those declarations fall back silently instead of using the system. | Code drift | Defined the slow motion token, rewrote stale aliases to current tokens, replaced the one-off spacing reference with the existing ladder, and added a live-token guard with named runtime/generated inputs. | `tokens.css`; renderer CSS; `tests/e2e/typography-tokens.spec.ts` |
| CA61 | Activity animations used raw per-file durations, so motion rhythm could drift even though transitions already used shared motion tokens. | Code drift | Added role tokens for spin, caret, working-dot, pulse, shape, and reduced-motion cycles; token/static coverage now fails raw motion timing literals outside token declarations and zero-delay resets. | `tokens.css`; renderer CSS; `tests/e2e/typography-tokens.spec.ts` |
| CA62 | Foundations duplicated a large `:root` token table that had already drifted from the live token layers, weakening the single-source token model. | Spec drift | Foundations now name the live CSS token layers as the value source and keep only token-family contracts; token/static coverage fails future design-system CSS examples that copy a root token table. | `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA63 | A vertical resize handle used a raw `ns-resize` cursor even though the native-feel contract names shared resize cursor tokens. | Code drift | Run-detail resizing now uses `--resize-cursor-y`, and cursor-affordance coverage fails future raw resize cursor literals. | `agent-run-detail.css`; `tests/e2e/cursor-affordances.spec.ts` |
| CA64 | Pointer-cursor coverage allowed any selector containing `.inline-ref`, so a future non-link control could inherit the hand cursor by naming alone. | Code drift | Cursor-affordance coverage now allows only the exact content-link inline-reference selectors that intentionally use the pointer cursor. | `tests/e2e/cursor-affordances.spec.ts` |
| CA65 | B6 was documented and spot-checked at runtime, but static validation did not stop named chrome icon controls from regaining hover/focus/press background boxes. | Code drift | Cursor-affordance coverage now fails non-transparent hover, focus, or press backgrounds on named chrome icon controls, and stale rail-collapse prose now describes colour deepening instead of a fill tint. | `tests/e2e/cursor-affordances.spec.ts`; `agent-dock.css`; `implementation.md` |
| CA66 | The Settings frameless drag region carried chrome title text but did not suppress text selection like the main window and pane breadcrumb drag regions. | Code drift | Settings drag chrome now has `user-select: none`, and cursor-affordance coverage fails any renderer `-webkit-app-region: drag` rule that remains selectable. | `settings-base.css`; `tests/e2e/cursor-affordances.spec.ts` |
| CA67 | Local `:focus-visible` rules suppressed the shared focus-ring shadow in editor-owned and structural-control paths, but those suppressions were implicit and could grow without review. | Named exception | Patterns now name the valid local focus mechanisms, and cursor-affordance coverage fails any unregistered `:focus-visible` `box-shadow: none` rule. | `patterns.md`; `tests/e2e/cursor-affordances.spec.ts` |
| CA68 | A few explicit `:focus-visible` indicators used generic emphasis or hover-named tokens, making keyboard focus look correct but weakening focus-token ownership. | Code drift | Definition config focus now uses `--focus-ring`, local-file reference focus has a focus-named component token, and cursor-affordance coverage fails future focus indicators that do not reference a focus token. | `outliner.css`; `inline-ref.css`; `tests/e2e/cursor-affordances.spec.ts` |
| CA69 | Material backgrounds were paired with the shared backdrop filter, but validation did not prove they stayed limited to chrome, popovers, menus, tooltips, and floating media/code controls. | Code drift | Token/static coverage now registers every current material-background selector and fails unregistered or stale material surfaces, preserving the two-layer chrome/overlay-only material model. | `tests/e2e/typography-tokens.spec.ts`; `implementation.md` |
| CA70 | `backdrop-filter` scope was value-guarded but not surface-guarded, so a future content surface could add shared blur without using a registered material surface or named HUD exception. | Code drift | Token/static coverage now fails backdrop filters outside registered material surfaces and the preview-HUD action exception, and fails stale preview-HUD exception entries. | `tests/e2e/typography-tokens.spec.ts`; `implementation.md` |
| CA71 | The kernel named `cursor: help` as a diagnostics-only exception, but cursor validation did not fail unrelated future help cursors. | Code drift | Cursor-affordance coverage now registers the debug-cost and field-value validation hint selectors, fails unregistered help cursors, and fails stale help-cursor exception entries. | `tests/e2e/cursor-affordances.spec.ts`; `implementation.md` |
| CA72 | The native cursor rule says text cursor belongs only on real text, but validation did not fail future `cursor: text` use on ordinary chrome or rows. | Code drift | Cursor-affordance coverage now registers text/editor selectors and fails unregistered or stale text-cursor declarations. | `tests/e2e/cursor-affordances.spec.ts`; `implementation.md` |
| CA73 | Floating rails repeated the same rail elevation plus edge-ring box-shadow recipe across sidebar, agent dock, and Settings rail instead of naming the shared rail surface shadow. | Code drift | Added `--rail-surface-shadow`, rewrote rail surfaces to use it, updated foundation prose, and added static coverage that fails future hand-written rail shadow recipes. | `tokens.css`; rail CSS; `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA74 | File preview HUD pills repeated the same strong thumb shadow plus preview outline ring even though the HUD already owns a named contrast token family. | Code drift | Added `--preview-action-shadow`, routed both HUD pills through it, clarified the component contract, and added static coverage that fails future hand-written HUD shadow recipes. | `tokens.css`; `file-preview.css`; `components.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA75 | Agent and error surfaces still referenced the legacy `--danger` alias instead of the semantic status token, weakening the one status-colour model. | Code drift | Replaced live `var(--danger)` references with `var(--status-danger)`, removed the alias, and added static coverage that fails future `--danger` definitions or references. | renderer CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA76 | Several legacy aliases remained defined even though live renderer CSS no longer used them, leaving extra names that future code could choose instead of the semantic layer. | Code drift | Removed unused aliases (`--app-bg`, `--accent-brand`, `--semantic-danger-muted`, `--semantic-info`, `--overlay-active-bg`) and added static coverage that fails their return. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts`; `foundations.md` |
| CA77 | Success and warning status styles still used `--semantic-*` aliases even though the owning colour layer is `--status-*`. | Code drift | Replaced non-outliner `--semantic-success`, `--semantic-success-strong`, and `--semantic-warning` references with `--status-*`, removed the aliases, and expanded retired-alias coverage. | agent CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts`; `foundations.md` |
| CA78 | `--border-muted` remained as a duplicate name for `--separator`, allowing separator strokes to drift back onto a border-named alias. | Code drift | Routed the remaining non-outliner use and the `--border` bridge directly to `--separator`, removed `--border-muted`, and expanded retired-alias coverage. | `agent-debug.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA79 | The generic `--border` alias still duplicated `--separator`, so separator strokes could keep using a broad legacy name instead of the owning stroke token. | Code drift | Routed the remaining non-outliner uses and input bridge directly to `--separator`, removed `--border`, and expanded retired-alias coverage. | `input.css`; agent CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA80 | `--text-body` duplicated `--text-primary`, so primary text could keep using an old role name instead of the foundation text ladder. | Code drift | Routed the remaining non-outliner uses to `--text-primary`, removed `--text-body`, and expanded retired-alias coverage. | sidebar/agent/settings CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA81 | `--overlay-bg` duplicated `--bg-elevated`, so opaque agent overlays could keep using a broad legacy alias instead of the owning elevated-surface token. | Code drift | Routed both agent overlay backgrounds to `--bg-elevated`, removed `--overlay-bg`, and expanded retired-alias coverage. | agent CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA82 | `--overlay-backdrop-strong` remained defined in light/dark token layers even though no live renderer surface used it. | Code drift | Removed the unused token definitions and expanded retired-alias coverage so a future strong scrim must be named with real usage and evidence. | `tokens.css`; `theme-dark.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA83 | `--deck-bg` duplicated `--bg-window` and survived only as the root background bridge. | Code drift | Routed the root background directly to `--bg-window`, removed `--deck-bg`, and expanded retired-alias coverage. | `tokens.css`; `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA84 | `--surface-soft` duplicated `--bg-elevated`, so local elevated surfaces and the `--bg` bridge could keep using a generic legacy surface name. | Code drift | Routed code/file-preview surfaces and the `--bg` bridge directly to `--bg-elevated`, removed `--surface-soft`, and expanded retired-alias coverage. | `code.css`; `file-preview.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA85 | Foundations treated neutral user bubbles as if they were colored semantic surfaces that need a dedicated foreground/background pair. | Spec drift | Clarified that current user bubbles are neutral content surfaces on `--fill-3` + `--text-strong`; only colored semantic surfaces need their own pair. | `foundations.md`; `agent-message.css` |
| CA86 | `--surface-user-bubble` duplicated `--fill-3` for one agent transcript surface after the design system settled on neutral user bubbles. | Code drift | Routed the user message bubble directly to `--fill-3`, removed `--surface-user-bubble`, and expanded retired-alias coverage. | `agent-message.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA87 | `--agent-accent` duplicated the single rose `--accent` token, leaving a second brand-colour name for an agent running badge even though the foundation owns one sparse accent. | Code drift | Routed the running badge directly to `--accent`, removed `--agent-accent`, and expanded retired-alias coverage. | `agent-dock.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA88 | Several compatibility aliases had no live renderer reference, leaving extra token names that future CSS could pick instead of the owning spacing, focus, shell, or control-size token. | Code drift | Removed the zero-reference aliases (`--breadcrumb-height`, `--focus-border`, `--search-builder-focus-shadow`, `--shell-padding-*`, `--shell-gap`, `--space-micro`, `--space-sm`, `--space-md`, `--workspace-tab-*`) and expanded retired-alias coverage. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA89 | The spacing spec and Settings layout comment still named retired gap aliases (`--space-sm`, `--shell-gap`) instead of the current `--layout-gap` / `--space-4` owner. | Spec drift | Updated the spec/comment to reference the owning layout-gap token directly, keeping the spacing rule reusable and measurable. | `foundations.md`; `settings-base.css` |
| CA90 | `--panel-gap` duplicated `--layout-gap` only for the workspace layout reader, so pane spacing had a second token name without an independent design role. | Code drift | The workspace layout reader now uses `--layout-gap` for pane gaps, `--panel-gap` is retired, and retired-alias coverage scans renderer TS/TSX string reads as well as CSS declarations/references. | `workspaceResponsiveLayout.ts`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA91 | `--checkbox-mark-radius` renamed `--radius-xs` for a single checkbox mark declaration even though the radius ladder already names that 3px mark role. | Code drift | Routed `CheckboxMark` directly to `--radius-xs`, removed the alias, and expanded retired-alias coverage. | `controls.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA92 | `--agent-dock-inset-x` duplicated the shared `--rail-pad` token inside the `--agent-content-x` formula, adding a second name for the same rail inset. | Code drift | Routed the agent content-column formula directly through `--rail-pad`, removed `--agent-dock-inset-x`, and expanded retired-alias coverage. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA93 | Foundations documented stale dock widths that no longer matched the live rail tokens and responsive-layout constants. | Spec drift | Updated the dock-width contract to `--sidebar-*` / `--agent-*` live values and added static coverage that fails future prose/token drift. | `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA94 | Rail width values are duplicated between design tokens and responsive-layout constants, but only the prose was guarded against drift. | Code drift | Added renderer unit coverage that fails if `workspaceResponsiveLayout` rail constants diverge from `--sidebar-*` / `--agent-*` tokens. | `tests/renderer/workspaceResponsiveLayout.test.ts` |
| CA95 | Button primary foreground still used legacy `--panel-bg` even though the owning content surface token is `--bg-content`. | Code drift | Routed the primary button foreground to `--bg-content` without changing the visual value. | `button.css` |
| CA96 | The Button component contract named the legacy `--panel-bg` alias as part of the filled-default idiom. | Spec drift | Updated the component contract to name the owning `--bg-content` token, keeping the primitive spec aligned with the semantic surface layer. | `components.md` |
| CA97 | `CheckboxMark` still carried a base `--panel-bg` colour even though the unchecked mark renders no glyph and the checked glyph already owns the fixed-white `--text-on-accent` token. | Code drift | Removed the unused base colour and kept checkbox/switch on-state comments tied to the fixed-white control glyph/knob contract. | `controls.css`; `components.md`; `foundations.md` |
| CA98 | The composer surface spec still said no renderer approval overlay was shipped even though approval and user-question cards are current in-composer product behavior. | Spec drift | Replaced the stale no-overlay warning with the current in-composer blocking-card contract, improving consistency with runtime/store/E2E evidence without changing UI. | `surfaces.md`; `AgentComposer.tsx`; `tests/e2e/agent-composer.spec.ts`; `tests/renderer/agentRuntimeStore.test.ts` |
| CA99 | Agent composer primary approval and send/stop actions used the legacy `--panel-bg` alias for inverse-button foregrounds. | Code drift | Routed those foregrounds to the owning `--bg-content` token, matching the filled-default button contract without changing the visual value. | `agent-composer.css`; `components.md`; `foundations.md` |
| CA100 | Non-outliner content surfaces, inverse-surface foreground gaps, and the legacy `--surface` bridge still referenced `--panel-bg` even though the owning opaque content token is `--bg-content`. | Code drift | Routed non-outliner content backgrounds, inverse foregrounds, swatch gap shadow, and the `--surface` bridge directly to `--bg-content`; the remaining outliner checkbox use stays for a separate cautious interaction review. | `agent-run-detail.css`; `agent-dock.css`; `panel.css`; `tokens.css`; `outliner.css` |
| CA101 | Non-outliner content panels, file-preview frames, debug cards, badges, and avatar tints still used the legacy `--surface` alias after the alias bridge had been narrowed to `--bg-content`. | Code drift | Routed those uses directly to `--bg-content`, leaving only the outliner-owned `--surface` / `--surface-2` uses for a separate cautious review. | `settings-provider-sheet.css`; `agent-debug.css`; `canvas.css`; `file-preview.css`; `agent-run-detail.css`; `agent-dock.css`; `agent-message.css`; `tokens.css`; `outliner.css` |
| CA102 | Foundations described tag tint mixing against the legacy `--surface` alias instead of the owning content-surface token. | Spec drift | Updated the tag palette contract to mix against `--bg-content`, matching tag/avatar implementation and making the tint rule refer to the semantic surface owner. | `foundations.md`; `agent-message.css`; `tagColors.ts` |
| CA103 | The last outliner references to `--panel-bg`, `--surface`, and `--surface-2` were same-value compatibility aliases for `--bg-content` / `--bg-window`, leaving three extra names for the same content/window surfaces. | Code drift | Routed the outliner uses to the owning semantic tokens, removed the aliases, and expanded retired-alias coverage so they cannot return unnoticed. | `outliner.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA104 | Foundations still described `--panel-bg` as a live legacy alias after the code path no longer needed it. | Spec drift | Reworded the legacy-alias rule around remaining aliases and retired-alias guard coverage, keeping the spec aligned with the token layer. | `foundations.md`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA105 | `--bg` still duplicated `--bg-elevated`, and `--tab-hover-bg` survived as a zero-reference alias, leaving broad names for surfaces with no independent contract. | Code drift | Routed the three live `--bg` uses to `--bg-elevated`, removed both aliases, and expanded retired-alias coverage. | `settings-base.css`; `outliner.css`; `toast-error.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA106 | `--text-muted` duplicated `--text-tertiary` without an independent component, state, or surface contract, leaving a second name for the same low-emphasis text level. | Code drift | Routed live uses to `--text-tertiary`, removed the alias, and expanded retired-alias coverage so low-emphasis text uses the foundation ladder directly. | renderer CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA107 | `--text-sub` duplicated `--text-secondary` for dialog copy, sidebar icons, calendar text, and a focused description editor without owning a reusable semantic role. | Code drift | Routed live uses to `--text-secondary`, removed the alias, and expanded retired-alias coverage so secondary text uses the foundation ladder directly. | renderer CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA108 | `--text-main` duplicated `--text-primary` in live CSS and legacy-alias prose without owning a separate text role. | Code drift | Routed live uses to `--text-primary`, removed the alias, expanded retired-alias coverage, and generalized the foundations legacy-alias sentence so the spec no longer cites a retired token. | renderer CSS; `tokens.css`; `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA109 | `--window-material-frost` remained defined after all material surfaces moved to the named `--material-*` and `--bg-window` layers. | Code drift | Removed the zero-reference alias and expanded retired-alias coverage so future window-material tints must be named through the material system. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA110 | `--surface-disabled` duplicated `--fill-3` for disabled buttons even though disabled functional state belongs to the neutral fill ladder. | Code drift | Routed disabled button/composer fills to `--fill-3`, removed the alias, and expanded retired-alias coverage. | `button.css`; `agent-composer.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA111 | Zero-reference tab compatibility tokens remained after the global workspace tab strip was removed, leaving unused names future CSS could choose instead of surface-specific primitives. | Code drift | Removed `--workspace-tab-width`, `--tab-bg`, and `--tab-active-bg`, and expanded retired-alias coverage so future tab-like controls must define their own component contract. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA112 | `--agent-side-panel-shadow` remained in light and dark token layers after agent side-panel elevation moved to the shared overlay/rail shadow tokens. | Code drift | Removed the zero-reference shadow token from both theme layers and expanded retired-alias coverage so future agent panel elevation must use a named live surface role. | `tokens.css`; `theme-dark.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA113 | `--agent-composer-corner-radius` remained as an unused derived radius after the composer contract settled on flush rail corners plus pill controls. | Code drift | Removed the zero-reference derived token and expanded retired-alias coverage so future composer geometry must use the live `--panel-radius` / `--agent-composer-corner-inset` contract. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA114 | Zero-reference collapsed rail width and outline ideal-width tokens remained even though current layout reads open/closed state from JS and only uses the panel minimum width token. | Code drift | Removed `--sidebar-collapsed-width`, `--agent-collapsed-width`, and `--outline-panel-ideal-width`, while keeping the documented rail min/max and panel min-width contracts guarded. | `tokens.css`; `workspaceResponsiveLayout.ts`; `tests/e2e/typography-tokens.spec.ts` |
| CA115 | Non-outliner surfaces still used the broad `--muted` / `--muted-2` aliases instead of the foundation text ladder, making ordinary secondary/tertiary text harder to audit. | Code drift | Routed non-outliner muted text to `--text-secondary` / `--text-tertiary`; the remaining outliner uses stay for a separate cautious row/text audit. | non-outliner renderer CSS; `tokens.css` |
| CA116 | Non-outliner surfaces still used the broad `--text` alias instead of `--text-primary`, hiding ordinary primary text and primary ink-mix usage behind a legacy name. | Code drift | Routed non-outliner primary text and ink mixes to `--text-primary`; the remaining outliner uses stay for a separate cautious row/text audit. | non-outliner renderer CSS; `tokens.css` |
| CA117 | Outliner still used same-value `--text`, `--muted`, and `--muted-2` compatibility aliases after non-outliner surfaces had moved to the foundation text ladder. | Code drift | Routed outliner text references to `--text-primary`, `--text-secondary`, and `--text-tertiary` without changing row geometry or interaction behavior; removed the aliases and expanded retired-alias coverage so future text roles use the reusable ladder directly. | `outliner.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA118 | Reused system-field rows displayed view-toolbar labels such as `Created time`, even though reusable field rows and the core system-field registry own the shorter fixed labels (`Created`, `Last edited`, `References`, etc.). | Code drift | Field rows now read labels from the core system-field registry; view toolbar choices keep their context-specific labels, preserving a clearer separation between row UI and view-configuration UI. | `OutlinerFieldRow.tsx`; `systemFields.ts`; `ui-behavior.md`; `tests/e2e/outliner-triggers.spec.ts` |
| CA119 | `--border-subtle` duplicated `--separator`, so ordinary hairlines could keep using a broad border-named alias after the spec and prior alias cleanup had made `--separator` the owner for in-content dividers. | Code drift | Routed ordinary border/hairline uses and `--inset-hairline` directly to `--separator`; removed the alias and expanded retired-alias coverage so future subtle strokes use the measurable separator contract. | renderer CSS; `tokens.css`; `tests/e2e/typography-tokens.spec.ts`; `foundations.md` |
| CA120 | `--text-disabled` duplicated `--text-quaternary`, adding a state-named compatibility alias for the same lowest-emphasis text level. | Code drift | Routed disabled text to `--text-quaternary`, updated the disabled-state pattern to name the foundation ladder directly, removed the alias, and expanded retired-alias coverage so disabled text stays measurable through the shared text scale. | renderer CSS; `patterns.md`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA121 | The day-panel calendar guard queried date cells as buttons even though the current calendar grid exposes selectable days as `gridcell` elements, matching the runtime design-system guard. | Code drift | Updated the focused outliner navigation guard to query day cells by `gridcell`, keeping the test aligned with the shipped calendar semantics without changing UI. | `PanelDateNavigation.tsx`; `CalendarMonthGrid`; `tests/e2e/outliner-navigation-title.spec.ts`; `tests/e2e/design-system-runtime.spec.ts` |
| CA122 | `--row-selection-bg` duplicated `--row-selected` for the outliner selection fill, adding a second name without an independent row-state contract. | Code drift | Routed the selected-row background directly to `--row-selected`, removed the alias, and expanded retired-alias coverage so row selection uses the single selected-state token. | `outliner.css`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA123 | `--agent-composer-radius` remained defined after the composer contract settled on a flush rail surface that reuses `--panel-radius`, not a nested-card radius. | Code drift | Removed the zero-reference token, updated foundations to keep `--panel-radius` as the surface-radius owner, and expanded retired-alias coverage so composer geometry does not reintroduce a duplicate radius role. | `tokens.css`; `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA124 | The foundations kept an informational Sapphire status role even though no current product surface uses an info status token. | Spec drift | Removed the unused `--status-info` light/dark tokens, updated the status rule and dark-mode plan to keep only product-used status roles, and expanded retired-token coverage so a future info-blue role requires current surface evidence. | `tokens.css`; `theme-dark.css`; `foundations.md`; `dark-mode-contrast-pass.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA125 | `--z-base` remained as a zero-reference z-index token even though local stacking contexts intentionally use raw `0` / `1` / `2` and the global ladder starts at raised surfaces. | Code drift | Removed the unused base token and expanded retired-alias coverage so the `--z-*` ladder only names global elevation tiers. | `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA126 | Token comments labeled `--text-strong`, `--text-soft`, and `--text-faint` as legacy aliases even though current transcript, tool, and dense chrome surfaces use them as shared reading registers. | Spec drift | Reclassified the trio as intentional register aliases over the foundation text ladder, keeping the underlying `--text-*` values as the single source while preventing future audits from treating the register vocabulary as cleanup debt. | `tokens.css`; `foundations.md`; `agent-event-log-rendering.md`; `tests/e2e/agent-composer.spec.ts` |
| CA127 | Foundations described remaining token aliases as compatibility shims to migrate opportunistically, even though current aliases are mostly reusable role contracts and legacy shims have been retired. | Spec drift | Replaced the stale compatibility-shim language with alias discipline: keep aliases only when they name reusable roles, and guard retired shims from returning. | `foundations.md`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA128 | The active dark-mode contrast plan still cited retired `--border-subtle` and `--text-disabled` aliases after those roles moved to `--separator` and `--text-quaternary`. | Spec drift | Updated the plan to use current token names so the future contrast pass measures the reusable owner tokens rather than retired compatibility names. | `dark-mode-contrast-pass.md`; `tokens.css`; `tests/e2e/typography-tokens.spec.ts` |
| CA129 | The active dark-mode contrast plan still cited deleted `agent-subagent.css` evidence after child-run/task styling moved into current agent tool and run-detail stylesheets. | Spec drift | Replaced the stale file references with current `agent-tool-rows.css`, `agent-run-detail.css`, and live disabled-control selectors so the future contrast pass can inspect real renderer surfaces. | `dark-mode-contrast-pass.md`; `agent-tool-rows.css`; `agent-run-detail.css`; `agent-composer.css` |
| CA130 | The active dark-mode contrast plan used brittle `file:line` evidence that had drifted away from the selectors and tokens it claimed to inspect. | Spec drift | Replaced line-number evidence with selector/token evidence so future visual confirmation re-greps current surfaces instead of trusting stale coordinates. | `dark-mode-contrast-pass.md`; renderer stylesheets |
| CA131 | Dark-mode `--text-tertiary` stayed at the light-mode 0.30 alpha, leaving real `--text-faint` text around 2.6–2.7:1 on dark content and material surfaces. | Code drift | Lifted `--text-tertiary` centrally to 0.40 only in dark mode and updated the dark contrast plan to treat the token lift as the reusable fix, not a per-surface exception. | `theme-dark.css`; `tokens.css`; dark contrast probe; `dark-mode-contrast-pass.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA132 | Raw-hex named exceptions were checked for registry documentation but not for stale source evidence, so a removed source literal could leave a dead exception in the metrics table. | Code drift | Metrics now report and fail stale raw-hex exceptions, matching the native-control exception discipline that named exceptions must stay current and evidence-backed. | `scripts/design-system-metrics.ts --json`; `implementation.md` |
| CA133 | Implementation guidance still described legacy aliases as migration bridges after the calibration pass retired compatibility shims and kept only reusable role aliases. | Spec drift | Reworded implementation maintenance around alias discipline: aliases must name reusable role contracts, while retired compatibility names stay guarded from returning. | `implementation.md`; `foundations.md`; `tests/e2e/typography-tokens.spec.ts` |
| CA134 | The active dark-mode contrast plan still described `--text-faint` menu/sidebar and launcher-dot risks as 0.30 dark ink after the central dark `--text-tertiary` lift moved that tier to 0.40. | Spec drift | Updated those plan rows to use the current dark 0.40 owner token while keeping the remaining task as visual confirmation of the same reusable tier, not a per-surface colour patch. | `dark-mode-contrast-pass.md`; `theme-dark.css` |
| CA135 | Component/native-control coverage scanned only `src/renderer/ui`, so renderer entry surfaces such as the launcher could add direct native controls without affecting component coverage. | Code drift | Metrics now scan renderer TS/TSX broadly while still separating primitive implementation internals and named native-control exceptions. | `scripts/design-system-metrics.ts --json`; `implementation.md`; `src/renderer/launcher/LauncherApp.tsx` |
| CA136 | Raw functional colour enforcement only scanned renderer CSS, so TS/TSX source-owned style strings could add `rgb()` / `rgba()` / `hsl()` literals without affecting the design-system metrics gate. | Code drift | Metrics now scan renderer CSS, TS, and TSX for raw functional colour literals, matching the renderer-wide raw-hex discipline and keeping source-owned style strings on tokens. | `scripts/design-system-metrics.ts --json`; `foundations.md`; `implementation.md` |
| CA137 | Validation prose still implied `typography-tokens.spec.ts` owned renderer-wide raw functional colour enforcement, even though that E2E guard only scans stylesheets and the renderer-wide scan now lives in metrics. | Spec drift | Split the validation wording: metrics owns renderer-wide raw colour discipline across CSS/TS/TSX, while typography-token E2E owns stylesheet/static CSS token discipline. | `implementation.md`; `scripts/design-system-metrics.ts`; `tests/e2e/typography-tokens.spec.ts` |
| CA138 | After raw functional colour scanning was added, the exception registry in metrics still only supported raw-hex exceptions, so a future unavoidable `rgb()` / `rgba()` source literal could not be named with the same evidence discipline. | Code drift | Generalized the metrics exception map, stale check, undocumented check, and reporting to raw-colour exceptions while preserving the current model-upload white-matting exception. | `scripts/design-system-metrics.ts --json`; `implementation.md` |
| CA139 | Raw functional colour evidence stopped at the first `)` and would report incomplete values for nested CSS functions such as `rgb(var(--ink) / 0.4)`, making future exception keys unstable. | Code drift | Replaced the regex-only value capture with balanced-parenthesis scanning so metrics report the full raw functional colour literal. | `scripts/design-system-metrics.ts --json` |
| CA140 | Exception Registry evidence coverage only checked for link/code-span syntax, so a moved spec, test, or source file could keep counting as evidence after the local reference broke. | Code drift | Metrics now fail broken local authority/evidence links and path-like evidence code spans, and registry evidence uses repo paths where source files are cited. | `scripts/design-system-metrics.ts --json`; `docs/spec/design-system.md`; `implementation.md` |
| CA141 | Raw-colour guards treated every CSS custom property as a token declaration, so a future component-private variable like `--local-bg: rgba(...)` could hide a raw colour outside the foundation layer. | Code drift | Metrics and typography-token guards now allow raw-colour declarations only in the foundation token layer (`tokens.css`, `theme-dark.css`, `a11y.css`); component-private variables must derive from tokens or become named exceptions. | `scripts/design-system-metrics.ts`; `tests/e2e/typography-tokens.spec.ts`; `foundations.md`; `implementation.md` |
| CA142 | Component-private shadow variables hand-wrote ring recipes even though shadow/stroke semantics belong to the token layer, and static token guards skipped custom-property definitions. | Code drift | Moved message highlight, inline-reference hover, and document-outline marker shadow recipes into token-layer role tokens; components now reference those tokens directly, and typography-token coverage fails future non-token-layer `--*shadow*` recipes. | `tokens.css`; `agent-message.css`; `inline-ref.css`; `file-preview.css`; `tests/e2e/typography-tokens.spec.ts`; `foundations.md` |
| CA143 | The spec allowed component `color-mix()` for local alpha states only when the base colour came from a token, but static coverage did not enforce that rule in component CSS or custom-property definitions. | Code drift | Typography-token coverage now fails non-token-layer `color-mix()` recipes whose base colour is not a token or `currentColor`, preserving local outliner separator mixes and inherited text dividers while making the derivation rule measurable. | `tests/e2e/typography-tokens.spec.ts`; `foundations.md`; `outliner.css`; `agent-tool-rows.css`; `agent-run-detail.css`; `sidebar.css` |
| CA144 | Cursor/native-feel coverage scanned stylesheet cursor declarations and runtime probes, but a future JSX inline style could still declare `cursor` outside the selector-based cursor policy. | Code drift | Cursor-affordance coverage now parses renderer TS/TSX JSX `style` attributes and fails inline cursor declarations, keeping cursor policy centralized in CSS selectors and named exceptions. | `tests/e2e/cursor-affordances.spec.ts`; `implementation.md` |
| CA145 | Inline file references used the hover shadow token for keyboard focus after shadow-recipe routing, weakening the focus-token ownership rule already enforced by cursor-affordance coverage. | Code drift | Routed local-file inline reference focus back to the shared `--inline-ref-focus-shadow`, keeping hover feedback and keyboard focus on distinct semantic tokens. | `inline-ref.css`; `tests/e2e/cursor-affordances.spec.ts`; `patterns.md` |
| CA146 | Native-feel coverage blocked cursor drift in JSX inline styles, but the same source-level escape hatch still existed for inline `userSelect` and `WebkitAppRegion` affordance properties. | Code drift | The cursor-affordance source parser now fails renderer JSX inline `cursor`, `userSelect`, `WebkitUserSelect`, and app-region properties so native affordance policy stays centralized in stylesheet selectors and named exceptions. | `tests/e2e/cursor-affordances.spec.ts`; `implementation.md` |
| CA147 | The decision audit required Exception rows to name a Kernel Exception Registry entry, but metrics only counted Exception rows and allowed generic `Exception Registry` references. | Code drift | Metrics now fails Exception decisions that do not name a registered exception, and all current Exception rows cite their exact kernel exception owner. | `scripts/design-system-metrics.ts --json`; `decision-audit.md`; `design-system.md`; `implementation.md` |
| CA148 | Decision-audit evidence used short file names and unvalidated descriptive evidence, so a file move or missing proof could still count as a Derived decision. | Code drift | Metrics now reports and fails decision-audit evidence coverage and broken local links/path references; decision evidence uses repo-relative paths where source files are cited. | `scripts/design-system-metrics.ts --json`; `decision-audit.md`; `implementation.md` |
| CA149 | The tooltip contract overgeneralized delayed opaque hover even though current shared tooltip implementations are immediate level-1 material overlays; delay is only established for pointer-delayed inline/file previews. | Spec drift | The tooltip rule now standardizes the reusable parts: read-only, non-interactive, level-1 material overlay, hover/focus reveal, instant dismiss; delayed previews remain in the inline/file-preview pattern. | `components.md`; `patterns.md`; `src/renderer/ui/outliner/ViewToolbar.tsx` |
| CA150 | Decision-audit metrics parsed only `Derived` / `Exception` rows, so a mistyped Result value could disappear from the denominator instead of failing the gate. | Code drift | Metrics now parses every `Dxx` row first and fails invalid decision Result values explicitly. | `scripts/design-system-metrics.ts --json`; `implementation.md` |
| CA151 | Decision-audit coverage could be improved by shrinking or renumbering the sample, because metrics did not enforce the current row baseline or stable ids. | Code drift | Metrics now keeps the current 50-row minimum and fails duplicate or non-contiguous `Dxx` ids, so the proof sample can grow but not silently shrink or hide a replaced row. | `scripts/design-system-metrics.ts --json`; `implementation.md` |
| CA152 | Native-control exceptions had a live metrics map and a hand-maintained calibration table, but no gate kept the two named-reason lists synchronized. | Code drift | Metrics now fails missing, extra, or reason-mismatched Native-Control Exceptions table rows, so exception accountability stays in one measurable loop. | `scripts/design-system-metrics.ts --json`; `calibration-audit.md`; `implementation.md` |
| CA153 | The agent debug usage tooltip used an inline `span` surface while rendering block usage rows inside it, weakening the shared tooltip surface semantics. | Code drift | The debug usage tooltip now uses a block `div` surface with the same `role="tooltip"`, material styling, and hover/focus behavior. | `src/renderer/ui/agent/AgentDebugPanel.tsx`; `tests/e2e/agent-debug-panel.spec.ts` |
| CA154 | Agent debug E2E still used the retired `--danger` alias as the expected error colour after status colour ownership moved to `--status-danger`. | Code drift | The test now compares error-result text against `--status-danger`, keeping test evidence aligned with the current status-token owner. | `tests/e2e/agent-debug-panel.spec.ts`; `tests/e2e/typography-tokens.spec.ts` |

## Named Exceptions Kept

These are intentional after calibration. They stay narrow and evidence-backed.

| Exception | Scope | Evidence |
| --- | --- | --- |
| External document pixels may force a light canvas. | HTML/EPUB/PDF document content inside file previews only. | Kernel Exception Registry; file-preview visual/token guards |
| Scoped dark-media rules are allowed for generated colour streams. | Generated colour systems such as Shiki; no renderer theme bridge. | Kernel Exception Registry; typography/token guards |
| Global launcher uses vibrant system glass. | Separate launcher window only. | Kernel Exception Registry; launcher spec |
| Preview HUD controls own contrast tokens. | File preview HUD over arbitrary pixels. | `--preview-action-*` tokens; file-preview checks; backdrop-scope guard |
| Composer model/effort chip is a profile shortcut. | Agent composer footer only. | Kernel Exception Registry; composer tests |
| Icon masks and provider logos are identity assets. | Inline-file masks and provider marks only. | Kernel Exception Registry; icon/token review |
| `cursor: help` is allowed for diagnostics. | Inline diagnostic hints or native-title tooltips only. | Cursor guard |
| Document outline mini-rail hides its scrollbar. | EPUB/document outline mini-rail track only; content scroll containers keep native lightweight scrollbars. | Kernel Exception Registry; hidden-scrollbar guard |
| Native window rounding depends on compiled addon output. | macOS app-window corner visual verification. | Kernel Exception Registry; window-material checks |
| Model-upload JPEG alpha matting may force a white canvas. | Agent composer image resizing for model upload only. | Raw-hex named exception in metrics |
| Local focus-ring suppression uses a named replacement indicator. | Editor-owned text canvases, clipped inset-card row inputs, non-tabstop outliner structural controls, and one compact text stop action only. | `focusVisibleRingSuppressionExceptions`; cursor-affordance guard |

## Native-Control Exceptions

Product surfaces prefer primitives. These direct native controls remain named
because they carry native/editor semantics, not because they are styling shortcuts.
The metrics JSON owns the live counts and stale-entry gate.

| File | Reason |
| --- | --- |
| `src/renderer/ui/agent/AgentComposer.tsx` | Hidden file input plus editor-owned buttons inside the composer surface. |
| `src/renderer/ui/agent/AgentComposerControls.tsx` | Hidden file input delegated to the composer attachment flow. |
| `src/renderer/ui/agent/AgentEditor.tsx` | Native textarea used by the agent-profile editor draft model. |
| `src/renderer/ui/agent/AgentMarkdown.tsx` | Checkbox input inside rendered markdown/task-list content. |
| `src/renderer/ui/agent/AgentMessageRow.tsx` | Textarea used for in-place message editing. |
| `src/renderer/ui/outliner/CodeBlockRow.tsx` | Textarea/select pair required for the code-block editor overlay. |
| `src/renderer/ui/outliner/DateValuePicker.tsx` | Native date/time controls inside the date picker. |
| `src/renderer/ui/outliner/NodeDescriptionSurface.tsx` | Textarea follows the outliner description editing model. |
| `src/renderer/ui/outliner/NodeValuePicker.tsx` | Input is an anchored filtering control with caller-owned query semantics. |

## Open Design Decisions

No new open design decision is accepted by this calibration pass. The audit
intentionally avoided changing outliner layout or interaction behavior without a
directional product decision. Future proposed changes in these areas must be
treated as open design decisions before implementation:

| Area | Why it needs a decision |
| --- | --- |
| Outliner row geometry, leading marker behavior, selection behavior, and hover affordances | These are core product interactions that have been tuned over many rounds. |
| Any new colour role beyond neutral state, one link blue, one rose accent, and status colours | This changes the system's semantic colour model. |
| Any new material layer or per-surface blur value | This changes the two-layer material model and reduced-transparency fallback. |
| Any new native-control exception | This changes component coverage accountability and must be named before use. |
